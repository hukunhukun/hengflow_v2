// Stable HengFlow terminal UI boundary. Keep renderer-specific dependencies
// isolated here so they can be internalized without changing product modules.
export { truncateToWidth, visibleWidth } from "@hengflow/tui";
